spring:
  cloud:
    config:
      #uri: http://icp-config-server:26020
      discovery:
        enabled: true
        service-id: icp-config-server
      fail-fast: true
      name: cc
      profile: config
      label:

eureka:
  client:
    service-url:
      defaultZone: http://peer1:26000/eureka/